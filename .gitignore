# macOS / editor
.DS_Store
.vscode/
.idea/

# Claude / Playwright / Shopify CLI local caches
.claude/
.playwright-mcp/
.shopify/

# Local screenshots and design references (not part of theme)
*.png
*.jpg
*.jpeg
*.gif
*.pdf
!assets/**/*.png
!assets/**/*.jpg
!assets/**/*.jpeg
!assets/**/*.gif

# Spreadsheets and CSV imports (local working files only)
*.xlsx
*.xls
*.csv
*.backup.csv
# Exception: the split-by-color CSV is a shipped artifact for the store admin to import
!products_split_by_color.csv

# Reference / clone theme (kept locally, not pushed)
miraggio-clone/

# Node / build artifacts (in case any tools add them later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
